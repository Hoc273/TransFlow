"""TransFlow AI microservice.

``__version__`` is the build version used as the Generated Asset Cache
``generator_version`` (ADR-CEP A2.1). The Docker image overrides it at build
time via ``APP_VERSION``; keep the constant in sync per release.
"""

__version__ = "0.2.0"
